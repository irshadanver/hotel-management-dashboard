/**
 * Revenue module — wire to backend when ready.
 * API_REQUIRED endpoints: see API_ENDPOINTS.revenue in lib/api/endpoints.ts
 *
 * Components still use inline mock data in components/revenue/*.
 * Replace with fetch calls following the pattern in services/dashboard.ts.
 */
import { API_ENDPOINTS } from "../endpoints";

export const REVENUE_API_NOTE = API_ENDPOINTS.revenue;
